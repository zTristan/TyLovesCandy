<%= form_for @photo, :html => { :multipart => true } do |f| %>
  <p>
    <% if @photo.image.exists? %>
      <%= image_tag @photo.image.url %><br/>
    <% end %>
    <%= f.file_field :image %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
