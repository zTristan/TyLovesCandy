<% if not @upload or @upload.new_record? %>
  alert("Failed to upload file: <%= j @upload.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#uploads").append("<%= j quick_edit(@upload) %>");
<% end %>
